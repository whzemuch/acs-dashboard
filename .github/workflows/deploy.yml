name: Deploy to GitHub Pages

permissions:
  contents: write

on:
  push:
    branches:
      - main
      - feat/data-provider-refactor-back

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Set mode + target branch
        id: set-mode
        run: |
          if [[ "${GITHUB_REF_NAME}" == "main" ]]; then
            echo "vite_mode=production" >> $GITHUB_OUTPUT
            echo "branch=gh-pages" >> $GITHUB_OUTPUT
          else
            echo "vite_mode=staging" >> $GITHUB_OUTPUT
            echo "branch=gh-pages-feature" >> $GITHUB_OUTPUT
          fi

      - run: npm ci

      - name: Build
        run: npm run build -- --mode ${{ steps.set-mode.outputs.vite_mode }}
        env:
          VITE_MAPBOX_TOKEN: ${{ secrets.VITE_MAPBOX_TOKEN }}

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: ${{ steps.set-mode.outputs.branch }}
